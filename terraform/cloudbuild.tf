# Cloud Build triggers have been removed.
# Trigger creation is blocked by an organisation-level policy on this
# Qwiklabs project. CI/CD can be configured manually via the GCP Console
# or re-enabled once the org constraint is lifted.
